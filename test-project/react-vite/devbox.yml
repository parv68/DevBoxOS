name: react-vite
version: "1.0"
runtimes:
    node: "18"
services:
    api:
        command: npm run dev
        port: "3000"
networking:
    discovery: true
    expose:
        - 3000
    egress: default-deny
