name: next-app
version: "1.0"
runtimes:
    node: "18"
services:
    api:
        command: npm run dev
        working_dir: E:\devboxos\test-project\next-app
        port: "3000"
networking:
    discovery: true
    expose:
        - 3000
    egress: default-deny
